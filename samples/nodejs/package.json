{
  "name": "connector-extension-sample-nodejs",
  "version": "1.0.0",
  "description": "Azure Functions Connector Extension - Node.js TypeScript sample",
  "type": "module",
  "main": "dist/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "start": "npm run build && func start",
    "prestart": "npm run build"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@azure/functions": "4.15.1-preview",
    "@azure/functions-extensions-connectors": "0.0.1-preview",
    "@azure/connectors": "0.1.1-preview"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
